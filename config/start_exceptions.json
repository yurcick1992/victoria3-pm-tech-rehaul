{
  "_comment": "Manual overrides for the 1836 start converter (tools/convert_history.ps1). Each rule targets a vanilla BASE building key, optionally scoped to a country (region_state tag, e.g. 'GBR') and/or state (e.g. 'STATE_HOME_COUNTIES'). action: 'force_tier' sets the factory to tier N (1-based) regardless of its vanilla PM; 'force_industry_tier' re-tiers it onto ANOTHER config industry's tier N (fields: industry, tier) — needed when the target chain is a separate industry whose base building never appears in vanilla history (the steamer chain); 'remove' deletes the factory from the start. Precedence: most specific scope wins (country+state > country > state > none); ties resolved by last rule. Only the 'rules' array is read; '_examples' is ignored. See config/start_baseline.json (generated by tools/extract_start.ps1) for the inventory of what exists to target.",
  "_examples": [
    { "building": "building_paper_mill", "country": "GBR", "action": "force_tier", "tier": 2 },
    { "building": "building_steel_mill", "country": "USA", "state": "STATE_PENNSYLVANIA", "action": "remove" }
  ],
  "_why_steamer_seed": "User-ruled 2026-08-16: the 1836 world has 105 clipper-shipyard levels and ZERO steamer supply, while screw_frigate (a tier-1-tech starting invention held by BEL/FRA/GBR/PRU/USA) unlocks the steamer-EATING building_port_steam from day 1 — mandated-subsidy runs bought phantom steamers at the +75% ceiling for decades. One shipyard per screw_frigate holder is seeded as building_shipyard_metal (their second-largest yard where they have several, their only one otherwise): 13 metal levels world-wide. Every recipient holds the unlocking tech at start, so L14 stays clean.",
  "rules": [
    { "building": "building_shipyard", "country": "GBR", "state": "STATE_HOME_COUNTIES", "action": "force_industry_tier", "industry": "shipyard_steam", "tier": 1 },
    { "building": "building_shipyard", "country": "FRA", "state": "STATE_BRITTANY", "action": "force_industry_tier", "industry": "shipyard_steam", "tier": 1 },
    { "building": "building_shipyard", "country": "USA", "state": "STATE_VIRGINIA", "action": "force_industry_tier", "industry": "shipyard_steam", "tier": 1 },
    { "building": "building_shipyard", "country": "BEL", "state": "STATE_FLANDERS", "action": "force_industry_tier", "industry": "shipyard_steam", "tier": 1 },
    { "building": "building_shipyard", "country": "PRU", "state": "STATE_WEST_PRUSSIA", "action": "force_industry_tier", "industry": "shipyard_steam", "tier": 1 }
  ]
}
