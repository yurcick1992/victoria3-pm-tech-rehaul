# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~

# Generated dev artifact (rebuilt by tools/extract_start.ps1 each build)
config/start_baseline.json

# Goods pictograms for the balance UI, generated by tools/extract_icons.ps1 each build.
# These are PARADOX ART converted from the game's .dds files - never commit them (the repo is
# public) and they are never shipped in mod/. The UI degrades gracefully if this file is absent.
ui/icons.js

# Self-contained balance-UI snapshots (tools/bundle_ui.mjs / balance-snapshot.cmd). Generated on
# demand from whatever ui/ currently holds, so committing one would be a second, silently stale copy
# of the config - and it is ~0.7 MB of inlined build output besides. Regenerate, never commit.
balance_ui_snapshot*.html

# Alternate/throwaway build outputs from build.ps1 -SaveTo <name> (mod_<name>/) and -DryRun
# (mod_dryrun_<pid>/, auto-deleted). Only the canonical mod/ is source. Note the trailing-slash
# and mod_ prefix keep the real mod/ tracked.
mod_*/

# Derived config variants (tools/testbed/make_vanilla_stub.ps1 and friends). Regenerated from
# config/mod_config.json, so they are outputs, not source. The pattern needs the dot: it matches
# mod_config.vanilla_stub.json and never mod_config.json itself.
config/mod_config.*.json
# ⚠ EXCEPT the era6 ladder, which is NOT a derivable output: it carries ~40 minutes of solved volumes
# from the re-banded ladder (ROADMAP step 1b) and exists only because the canonical config is frozen
# until the research-events batch has built. Losing it would mean re-solving, not re-generating.
!config/mod_config.era6.json
# The SAME rule for a variant config's derived artifacts. MOD_CONFIG redirects era_prices.json and
# era_presets.json to suffixed names (§10.60.2's note in era_scenarios.mjs), so a redirected solve drops
# a matching pair beside the canonical ones — outputs of an output. The un-suffixed pair stays tracked,
# because those two ARE committed design inputs.
config/era_prices.*.json
config/era_presets.*.json

# Output of testbed runs: per-run telemetry plus a copy of the game's own logs. Ignored because it
# is BULKY (game logs, minidumps), NOT because it is disposable - the harness itself
# (tools/testbed/*.ps1) and the schedule specs (tools/testbed/schedules/*.json) ARE source and stay
# tracked.
#
# !! sessions/ IS NEVER DELETED, not even when a run is obsoleted by a design change. A session is a
# historical observation - a specific game version, a config that may no longer exist, specific RNG
# playthroughs - so re-running does NOT reproduce it. Conclusions go in FINDINGS.md; the data stays
# here. See CLAUDE.md -> "TESTBED SESSIONS ARE NEVER DELETED" for the batch this rule cost us.
# sessions/ is the ONE results root. runs/ and batches/ are legacy roots from earlier drivers
# (run_observer's own default and the removed run_batch.ps1); kept ignored so a stale folder or an
# old invocation can never drop game logs into the tracked tree.
tools/testbed/sessions/
tools/testbed/runs/
tools/testbed/batches/

# Savegames pulled aside for analysis, and everything derived from them. Same rule as sessions/:
# ignored because it is ENORMOUS, not because it is disposable - one 1925 autosave is 45 MB and its
# gamestate inflates to 259 MB, so a folder of three is comfortably over 800 MB. A save is a
# historical observation too (that campaign, that seed, that game version), so do not delete these
# during a cleanup; the READINGS go in FINDINGS.md. The readers themselves (tools/testbed/save_*.mjs,
# lib_savebin.mjs) ARE source and stay tracked.
tools/testbed/saves_*/
*.gamestate

# Generated status board (CLAUDE.md -> Status board). It is a VIEW rebuilt from current state each
# session and published as an Artifact - never a document that accumulates, so it must not be
# committed or it will rot into a stale second source of truth. Normally it is written to the
# scratchpad, outside the repo entirely; these patterns are the safety net for when it is not.
# The TEMPLATE is deliberately NOT ignored - it is the reusable shell, and carries no state.
status_board*.html
STATUS_BOARD*.html
!tools/status_board.template.html

# mod/ and most of tools/ are source and belong on GitHub.
# The deployed mod excludes docs/tools automatically because the junction points at mod/ only.

# Third-party binaries fetched into the tree (rakaly melter). Not ours to redistribute; the
# EXACT release and how to re-fetch it are recorded in TESTBED_METRICS.md so the repo can be retraced.
tools/vendor/

# melted saves + their extracts: hundreds of MB, reproducible from the archived .v3
tools/testbed/_score_work/
tools/testbed/saves_debut/
tools/testbed/saves_x10_extra/

# The handover is a BATON, not a record (user ruling 2026-08-12). It is written at the end of a session
# for the next one and replaced wholesale; a committed copy rots into a stale second source of truth,
# exactly like a status board. Anything in it that must outlive the session belongs in FINDINGS.md
# (results), ROADMAP.md (plan), BALANCE_FRAMEWORK.md (rulings) or TESTBED_LANDMINES.md (guardrails).
HANDOVER.md

# alternate-config siblings: emit_techs.mjs derives this path from the config name, so an alt config
# needs one. Byte copies of the committed tree; regenerate with cp, never commit 585 KB twice.
config/tech_tree_options.*.json

# The four-rung canon (BALANCE_FRAMEWORK §10.69, 2026-09-03) is SOURCE, like era6 — un-ignored explicitly, with its L20 twin.
!config/mod_config.canon-4rung.json
# canon4-je: canon-4rung + the ruled research-event parameters (§10.69), the arm under test in canon4-je-n5 (2026-09-03)
!config/mod_config.canon4-je.json
!config/tech_tree_options.canon4-je.json
# canon4-art: the art-academy stress test (§10.70, 2026-09-04)
!config/mod_config.canon4-art.json
!config/tech_tree_options.canon4-art.json
# canon4v: the vanilla-first four-rung candidate (§10.71, 2026-09-04)
!config/mod_config.canon4v.json
!config/tech_tree_options.canon4v.json
!config/tech_tree_options.canon-4rung.json
# Ledger fill working dirs (tools/testbed/ledger/out_<batch>/): build products of the fill scripts; the published REPORT.html
# is copied into the session folder, which is the record.
tools/testbed/ledger/out_*/
# hand backups left beside configs and scripts during a re-derivation
config/*.precap
config/*.prefix_bak
config/*.premargin
tools/testbed/ledger/*.canon6
# canon4v-art3: canon4v with the film academy at x3 output (user-ruled 2026-09-04)
!config/mod_config.canon4v-art3.json
!config/tech_tree_options.canon4v-art3.json
# THE SIX-RUNG CANON, retired 2026-09-05 when canon4v became config/mod_config.json (BALANCE_FRAMEWORK §10.74): source, not derivable
!config/mod_config.six_rung.json
!config/tech_tree_options.six_rung.json
# canon4v-hai3: the ruled set on the canon — ai_value 3^era everywhere + the investment-pool defines (§10.75, 2026-09-05)
!config/mod_config.canon4v-hai3.json
!config/tech_tree_options.canon4v-hai3.json
# the 60-run plan of §10.76 (2026-09-06): the canon with the industry research bar at 24 months, and its A 2.2 fallback
!config/mod_config.canon-je24.json
!config/tech_tree_options.canon-je24.json
!config/mod_config.canon-je24-a22.json
!config/tech_tree_options.canon-je24-a22.json
!config/mod_config.canon-flat-in12.json
!config/tech_tree_options.canon-flat-in12.json
!config/mod_config.canon-flat-in13.json
!config/tech_tree_options.canon-flat-in13.json
# THE TWO TEST BOOKS OF THE 2+1 TESTS (user-ruled 2026-09-13, BALANCE_FRAMEWORK §10.78 / HANDOVER): generated by the era-keyed make_ab_config, source of a scheduled batch
!config/mod_config.canon-flat-in12-a16.json
!config/tech_tree_options.canon-flat-in12-a16.json
!config/mod_config.canon-flat-in12-a19.json
!config/tech_tree_options.canon-flat-in12-a19.json
!config/mod_config.canon-c16-in12.json
!config/tech_tree_options.canon-c16-in12.json
!config/mod_config.canon-c19-in12.json
!config/tech_tree_options.canon-c19-in12.json
!config/mod_config.probe-b16-slide.json
!config/tech_tree_options.probe-b16-slide.json
!config/mod_config.probe-accel-slide.json
!config/tech_tree_options.probe-accel-slide.json
!config/mod_config.canon-c19-in12-elec.json
!config/tech_tree_options.canon-c19-in12-elec.json
!config/mod_config.canon-c19-in13.json
!config/tech_tree_options.canon-c19-in13.json
!config/mod_config.canon-c205-in13.json
!config/tech_tree_options.canon-c205-in13.json
!config/mod_config.canon-c19-e0ai500.json
!config/tech_tree_options.canon-c19-e0ai500.json
!config/mod_config.canon-c19-in12-eager.json
!config/tech_tree_options.canon-c19-in12-eager.json
!config/mod_config.canon-c205-e0ai500-eager.json
!config/tech_tree_options.canon-c205-e0ai500-eager.json
!config/mod_config.canon-c205-in12-eager.json
!config/tech_tree_options.canon-c205-in12-eager.json
!config/mod_config.canon-je24-a22-in12.json
!config/tech_tree_options.canon-je24-a22-in12.json
# fit_cost_drivers.mjs re-derives this pool cache from the sessions (--refresh); 1.7 MB of derived data
tools/testbed/ledger/cost_drivers_pool.json
!config/mod_config.canon-c195-in12-eager.json
!config/tech_tree_options.canon-c195-in12-eager.json
!config/mod_config.canon-c195-in12.json
!config/tech_tree_options.canon-c195-in12.json
!config/mod_config.canon-c20-in12-eager.json
!config/tech_tree_options.canon-c20-in12-eager.json
!config/mod_config.canon-c19-in12-eager-sharp.json
!config/tech_tree_options.canon-c19-in12-eager-sharp.json
!config/mod_config.canon-c195-in12-eager-sharp.json
!config/tech_tree_options.canon-c195-in12-eager-sharp.json
# THE FOUR-BATCH PLAN OF 2026-09-16 (HANDOVER.md; awaiting the ruling): the two prepared books, the record of the proposal
!config/mod_config.canon-c19-e2soft.json
!config/tech_tree_options.canon-c19-e2soft.json
!config/mod_config.canon-b18-gm.json
!config/tech_tree_options.canon-b18-gm.json
!config/mod_config.canon-a19-gm.json
!config/tech_tree_options.canon-a19-gm.json
!config/mod_config.canon-a20-gm.json
!config/tech_tree_options.canon-a20-gm.json
!config/mod_config.canon-a205-gm.json
!config/tech_tree_options.canon-a205-gm.json
!config/mod_config.canon-c19-in12-mild.json
!config/tech_tree_options.canon-c19-in12-mild.json
# ROADMAP step 9 batch 1 — the margin-compression book: per-industry era-0 levelling (+5%) then A 1.9 / B 1.7,
# so every industry runs the SAME ladder (5 / 17 / 31 / 47 by era, target_be 95 / 85 / 76 / 68 in all 56 rungs).
!config/mod_config.canon-lvl-a19b17.json
!config/tech_tree_options.canon-lvl-a19b17.json

# THE EARLY-GAME in0 PROBE (2026-09-19, schedule in0_early_probe.json): the canon with ONE field moved, the era-0
# input penalty, at 1.3 / 1.4 / 1.5 against the canon's 1.2. Regenerable from each file's own _ab.command, kept
# because a finding cites them.
!config/mod_config.probe-in13.json
!config/tech_tree_options.probe-in13.json
!config/mod_config.probe-in14.json
!config/tech_tree_options.probe-in14.json
!config/mod_config.probe-in15.json
!config/tech_tree_options.probe-in15.json

# THE GRADED ERA-0 PENALTY PROBE (2026-09-19, schedule in0_graded_probe.json): the canon with --in0-stage,
# the penalty graded by how manufactured each industry's own inputs are. Regenerable from each _ab.command.
!config/mod_config.probe-grada.json
!config/tech_tree_options.probe-grada.json
!config/mod_config.probe-gradb.json
!config/tech_tree_options.probe-gradb.json
!config/mod_config.probe-gradc.json
!config/tech_tree_options.probe-gradc.json
!config/mod_config.probe-gradrev.json
!config/tech_tree_options.probe-gradrev.json


# THE SUPPLIER-SIDE TERM probe book (2026-09-19): grad-a plus --in0-supplier 0.5, the 1836 weights.
!config/mod_config.probe-gradsup.json
!config/tech_tree_options.probe-gradsup.json
!config/mod_config.probe-slide-mild.json
!config/tech_tree_options.probe-slide-mild.json
!config/mod_config.probe-slide-mild-c195.json
!config/tech_tree_options.probe-slide-mild-c195.json
!config/mod_config.probe-sm-b152.json
!config/tech_tree_options.probe-sm-b152.json
!config/mod_config.probe-sm-b155.json
!config/tech_tree_options.probe-sm-b155.json
!config/mod_config.probe-sm-b158.json
!config/tech_tree_options.probe-sm-b158.json
!config/mod_config.probe-sm-b162.json
!config/tech_tree_options.probe-sm-b162.json

# THE CANON since 2026-09-23 (user-ruled "Canonize the config with 1.58"): byte-identical to probe-sm-b158,
# which stays as the record of the arm the B gradient measured. config/mod_config.json is a verbatim copy of this.
!config/mod_config.canon-slide-b158.json
!config/tech_tree_options.canon-slide-b158.json

# ROADMAP step 11 / FINDINGS F159: the canon + the per-good trade weights (goods_traded_quantity), nothing else.
# Generated by tools/make_trade_config.mjs (the command is the file's _trade_variant.command).
!config/mod_config.canon-slide-b158-trade.json
!config/tech_tree_options.canon-slide-b158-trade.json
# The trade x1.5 B ladder (user-ruled 2026-09-24): the canon at B 1.64 / 1.68 / 1.72 / 1.76 + make_trade_config --scale 1.5.
# Schedule tools/testbed/schedules/trade15_bladder_n12.json. The slide-b1xx bases stay ignored (regenerable from _ab.command).
!config/mod_config.trade15-b164.json
!config/tech_tree_options.trade15-b164.json
!config/mod_config.trade15-b168.json
!config/tech_tree_options.trade15-b168.json
!config/mod_config.trade15-b172.json
!config/tech_tree_options.trade15-b172.json
!config/mod_config.trade15-b176.json
!config/tech_tree_options.trade15-b176.json
# THE CANON SINCE 2026-09-25 (user: "Canonize trade x1, B 1.64."): the previous canon at B 1.64 + the x1 trade table.
!config/mod_config.canon-b164-trade.json
!config/tech_tree_options.canon-b164-trade.json
# corpus_extract.mjs output: one line per usable century run, re-derivable from the sessions (FINDINGS F165)
tools/testbed/ledger/corpus_runs.jsonl
