# AUTO-GENERATED by tools/emit_research_events.mjs - do not edit by hand.
pmr_army_consc = { value = army_size_including_raised_conscripts }

pmr_mob_share = {
	value = 0
	every_scope_general = { add = num_mobilized_battalions }
	if = { limit = { pmr_army_consc > 0 }  divide = pmr_army_consc }
}

pmr_src_aniline_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_textile_mill }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_aniline_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_arc_welding_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_shipyard_metal_oil_fired }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_art_silk_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_synthetics_plant_indigo }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_autofrettage_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_artillery_foundry_recoiled }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5250
}

pmr_src_automatic_bottle_blowers_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_automatic_cartridge_lines_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_munition_plant_drawn_brass }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_baking_powder_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_food_industry_sweetener }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_bessemer_process_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_steel_mill }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_bolt_action_rifles_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_arms_industry_repeaters }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_breech_loading_artillery_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_artillery_foundry_smoothbore }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_canneries_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_cemented_carbide_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_tooling_workshop_rubber }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_chemical_bleaching_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_paper_mill_sulfite }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_combustion_engine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_urban_center }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_compression_ignition_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_railway_electric }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_concrete_quays_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_port_industrial }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_continuous_nitration_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_explosives_factory_vacuum }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 4500
}

pmr_src_continuous_strip_mill_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_steel_mill_electric_arc }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_conveyors_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_automotive_industry }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_deep_water_docks_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_port_steam }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_diesel_engine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_motor_industry_electric }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_dough_rollers_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_food_industry_baking_powder }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_drawn_brass_cartridges_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_munition_plant_shells }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_dynamite_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_explosives_factory }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_electric_arc_process_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_steel_mill_open_hearth }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_electric_motors_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_motor_industry }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_electric_railway_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_railway_steam }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_electrical_capacitors_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_textile_mill_sewing }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_electrical_generation_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_power }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_electrical_generation_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_ranching }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_electrical_generation_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_electrical_generation_3 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_service }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_electrical_generation_4 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_logging }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_explosive_shells_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_munition_plant }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_flash_freezing_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_fishing }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_flash_freezing_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_whaling }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_fractional_distillation_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_fractional_distillation_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_staple_crops }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_fractional_distillation_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_high_speed_diesel_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_motor_industry_diesel }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_high_speed_papermaking_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_paper_mill_kraft }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_high_speed_steel_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_tooling_workshop_steel }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_improved_fertilizer_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_chemical_plant }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_intensive_agriculture_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_staple_crops }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_iron_screw_steamers_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_port }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_iron_screw_steamers_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_fishing }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_iron_screw_steamers_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_whaling }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_kraft_process_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_paper_mill_bleached }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_marine_steam_turbine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_shipyard_metal }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_mechanical_tools_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_mechanical_tools_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_ranching }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_mechanical_tools_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_mechanised_cargo_handling_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_port_modern }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_mechanized_farming_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_staple_crops }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_mechanized_farming_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_ranching }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_mechanized_workshops_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_textile_mill_dye }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_mechanized_workshops_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_furniture_manufactory_lathe }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_nitrocellulose_lacquer_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_furniture_manufactory_mechanized }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5500
}

pmr_src_nitrogen_fixation_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_chemical_plant_improved }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_nitroglycerin_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_mining }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_oil_fired_boilers_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_shipyard_metal_turbine }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_oil_turbine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_power_plant_pulverized }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1025
}

pmr_src_open_hearth_process_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_steel_mill_bessemer }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_ostwald_process_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_explosives_factory_ammonia }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_pasteurization_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_ranching }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_pasteurization_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_fishing }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_pasteurization_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_whaling }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_plastics_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_glassworks_crystal }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_polyamide_synthesis_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_synthetics_plant_viscose }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_pulverized_coal_firing_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_power_plant }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_pumpjacks_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_oil_extraction }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_pumpjacks_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_agriculture }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_pumpjacks_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_pumpjacks_3 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_rubber }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_radio_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_electrics_industry }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_railways_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_mining }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_railways_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_railways_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_staple_crops }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_railways_3 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_logging }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_railways_4 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_oil_extraction }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_recoil_carriages_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_artillery_foundry_breech }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5250
}

pmr_src_regenerative_furnace_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_glassworks_leaded }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_repeaters_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_arms_industry_rifles }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_ribbon_machine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_glassworks_plastics }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_rifling_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_arms_industry }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_rotary_valve_engine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_rotary_valve_engine_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_heavy_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_rotary_valve_engine_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_military_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_rotary_valve_engine_3 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_rubber_mastication_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_rubber }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_screw_frigate_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_shipyard }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_shell_gun_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_artillery_foundry }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_stamped_receivers_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_arms_industry_bolt_action }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_steam_donkey_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_mining }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_steam_donkey_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_logging }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_steam_reforming_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_chemical_plant_nitrogen }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_steam_turbine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_manufacturing }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_steamship_bunkering_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_port }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_steel_railway_cars_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_railway }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 1000
}

pmr_src_steel_toolmaking_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_tooling_workshop_pig_iron }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_synthetic_indigo_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_synthetics_plant }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_telephone_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_trade_center }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_telephone_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_financial_district }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_threshing_machine_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_staple_crops }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_transfer_machining_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_automotive_industry_mass }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_vacuum_canning_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_vacuum_tube_electronics_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_type = building_electrics_industry_radio }
		add = { value = this.level  multiply = occupancy }
	}
		multiply = 5000
}

pmr_src_vulcanization_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_watertube_boiler_0 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_light_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_watertube_boiler_1 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_heavy_industry }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_watertube_boiler_2 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_mining }
		add = { value = this.level  multiply = occupancy }
	}
}

pmr_src_watertube_boiler_3 = {
	value = 0
	every_scope_building = {
		limit = { is_building_group = bg_plantations }
		add = { value = this.level  multiply = occupancy }
	}
}
