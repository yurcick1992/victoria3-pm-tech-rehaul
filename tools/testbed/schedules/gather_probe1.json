{
  "schedule_version": 1,
  "label": "gather-probe1",
  "_why": "INSTRUMENT PROBE, not measurement: verify the 'lost markets' collection route for the batch ledger's placeholder (F57/F60 report). The origins metric exists and threaded origin_goods since L5's fix; this probe confirms it emits parseable per-market import-origin lines for the LADDER'S goods on the current flatcost config arm, so the next full batch can carry it. Diagnostic span only.",
  "_arms": "flatcost = {kind: config, config: config/mod_config.flatcost.json} - the same arm as 20260815_153825 so the probe reads the design under test. Regeneration recipe in schedules/flatcost_n1.json _arms.",
  "_cost": "1 run x 3 in-game months, ~6-8 min wall.",
  "defaults": {
    "metrics": ["origins", "country_state"],
    "autosave_interval": "never",
    "timeout_minutes": 20,
    "tags": ["GBR", "FRA", "USA", "PRU", "RUS", "TUR"],
    "origin_goods": ["tools", "steel", "engines", "paper", "glass", "clothes", "small_arms"]
  },
  "setups": {
    "flatcost": { "kind": "config", "config": "config/mod_config.flatcost.json" }
  },
  "runs": [
    { "setup": "flatcost", "until": "1836.4.1", "dump_dates": ["1836.2.1", "1836.3.1"] }
  ]
}
